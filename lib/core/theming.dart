// import 'package:flutter/material.dart';
// import 'package:flutter_screenutil/flutter_screenutil.dart';

// ThemeData themeData(BuildContext context) => ThemeData(
//     scaffoldBackgroundColor: Colors.white,
//     visualDensity: VisualDensity.adaptivePlatformDensity,
//     textTheme: const TextTheme());

// ///wrap around the normal [TextStyle] and set constants
// TextStyle _style({
//   required double size,
//   FontWeight weight = FontWeight.w400,
//   Color color = Colors.black,
// }) =>
//     TextStyle(fontSize: size.sp, fontWeight: weight, color: color);

// ///Expose a context-free textTheme and theme constant
// final TextTheme textTheme = themeData(navKey.currentContext!).textTheme;
// final ThemeData theme = themeData(navKey.currentContext!);
